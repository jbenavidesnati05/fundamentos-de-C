#include <stdio.h>

#define PIZZACOST = 15;

int main(){

    float costoPizzas;
    float numberOfSlices = 3;
    costoPizzas = PIZZACOST * numberOfSlices;
    printf("total bill %f", costoPizzas);
    printf(newLine);

}